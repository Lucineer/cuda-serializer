[package]
name = "cuda-serializer"
version = "0.1.0"
edition = "2021"
description = "Binary encoding with message framing"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Lucineer/cuda-serializer"
readme = "README.md"
keywords = ["ai", "fleet", "agent", "rust"]
categories = ["science"]

[dependencies]
serde = { version = "1", features = ["derive"] }

